# Copy to .env. Keep version tags or digests for predictable upgrades.
OMNIVOICE_IMAGE=ghcr.io/valentinealan/wyoming-omnivoice:1.1.0
OMNIVOICE_DATA_DIR=./data
# Set to the Docker host's LAN address to restrict the listener.
OMNIVOICE_BIND_IP=0.0.0.0
OMNIVOICE_PORT=10200
