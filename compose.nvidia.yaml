services:
  wyoming-omnivoice:
    environment:
      OMNIVOICE_DEVICE: cuda
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
